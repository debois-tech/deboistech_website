import type { Project } from "@/lib/types";

export function ProjectCard({ project }: { project: Project }) { return <article className="card flex flex-col"><div className={`flex h-12 w-12 items-center justify-center rounded-xl ${project.icon_bg} ${project.icon_color}`} dangerouslySetInnerHTML={{ __html: project.icon_svg }} /><h3 className="mt-5 text-lg font-bold text-gray-900">{project.title}</h3><p className="mt-2 text-sm text-gray-500">{project.description}</p><div className="mt-4 flex flex-wrap gap-2">{project.tech_stack.map((tech) => <span key={tech} className="rounded-full bg-gray-100 px-2.5 py-1 text-xs text-gray-600">{tech}</span>)}</div>{project.link_url && <a href={project.link_url} target={project.link_target} rel="noopener noreferrer" className="mt-5 text-sm font-semibold text-primary-600 hover:text-primary-700">{project.link_label}</a>}{project.image_url && <img src={project.image_url} alt={project.image_alt || project.title} className="mt-5 max-h-48 w-full rounded-xl object-cover" />}</article>; }
