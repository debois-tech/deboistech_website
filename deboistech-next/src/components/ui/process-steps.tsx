export function ProcessSteps({ steps = [] }: { steps?: Array<{ title: string; description: string }> }) { return <div className="grid gap-8 md:grid-cols-3">{steps.map((step, index) => <div key={step.title} className="card"><span className="text-sm font-bold text-primary-600">0{index + 1}</span><h3 className="mt-4 text-lg font-bold text-gray-900">{step.title}</h3><p className="mt-2 text-sm leading-6 text-gray-500">{step.description}</p></div>)}</div>; }
