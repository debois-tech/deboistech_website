export function Careers({ roles = [] }: { roles?: Array<{ title: string; location?: string }> }) { return <div className="space-y-4">{roles.map((role) => <div key={role.title} className="card flex items-center justify-between"><div><h3 className="font-bold text-gray-900">{role.title}</h3><p className="mt-1 text-sm text-gray-500">{role.location ?? "Remote"}</p></div><span className="text-primary-600">→</span></div>)}</div>; }
